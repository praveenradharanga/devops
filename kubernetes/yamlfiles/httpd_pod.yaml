apiVersion: apps/v1
kind: ReplicaSet
metadata:
 name: hhtpd-pod
spec:
 replicas: 5
 selector:
  matchLabels:
   app: httpd-label
 template:
  metadata:
   labels:
    app: httpd-label
  spec:
   containers:
    - name: httpd
      image: httpd:2.4
      ports:
       - containerPort: 80
